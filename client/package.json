{
  "devDependencies": {
    "jest": "^29.5.0",
    "jsdom": "^22.0.0",
    "live-server": "^1.2.2"
  },
  "scripts": {
    "dev": "live-server",
    "test": "jest --watchAll",
    "coverage": "jest --coverage"
  }
}
